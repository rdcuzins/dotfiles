return {
	settings = {

		Lua = {
			diagnostics = {
        -- makes diagnostics shutup about vim not being found
				globals = { "vim" },
			},
			workspace = {
				library = {
					[vim.fn.expand("$VIMRUNTIME/lua")] = true,
					[vim.fn.stdpath("config") .. "/lua"] = true,
				},
			},
		},
	},
}
